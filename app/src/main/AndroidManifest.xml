<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dicoding.readme">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ShActivity"></activity>
        <activity android:name=".DilanActivity" />
        <activity android:name=".HujanActivity" />
        <activity android:name=".HereActivity" />
        <activity android:name=".KataActivity" />
        <activity android:name=".LpActivity" />
        <activity android:name=".GwActivity" />
        <activity android:name=".NkcthiActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".SeActivity" />
        <activity android:name=".BshActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>